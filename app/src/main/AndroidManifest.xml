<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andreskaminker.iuvocare">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/iuvo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iuvo_launcher_round"
        android:supportsRtl="true"
        android:name="com.andreskaminker.iuvocare.IuvoCareApplication"
        android:theme="@style/AppTheme">
        <activity android:name="com.andreskaminker.iuvocare.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.andreskaminker.iuvocare.MainActivity">

        </activity>
    </application>

</manifest>